name: clox

on:
  push:
    paths: [ 'clox/**' ]
    branches: [ main ]
  pull_request:
    paths: [ 'clox/**' ]
    branches: [ main ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            cache-path: "~/.cache/bazel"
            search-path: "~/.cache/bazel"
          - os: macos-latest
            cache-path: "/private/var/tmp"
            search-path: "/private/var/tmp"
          - os: windows-latest
            cache-path: "$HOME"
            search-path: "$USERPROFILE"
    defaults:
      run:
        shell: bash
        working-directory: clox

    steps:
    - uses: actions/checkout@v3
    
    - uses: bazelbuild/setup-bazelisk@v2

    - name: Mount bazel cache
      uses: actions/cache@v3
      with:
        path: ${{ matrix.cache-path }}/_bazel_${{ runner.name }}
        key: bazel-${{ matrix.os }}
    
    - name: Test
      run: bazel test --test_output=all ...

    - name: Find cache
      run: ls -la ${{ matrix.search-path }}