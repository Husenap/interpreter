#include "swing/jframe.lox"
#include "swing/jbutton.lox"

var frame = JFrame("Lox");

var button = JButton("Click Me!");

button.onClick(fun(){
  println("I was clicked from a lambda!");
});

frame.add(button);
frame.show();
